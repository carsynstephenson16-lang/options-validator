# Pinned false positives (gitleaks fingerprints: commit:file:rule:line).
#
# The A2 pin-fact ledger label constant in options_researcher/a2_runner.py: a
# public, tracked, documented ledger fact-type label held in a variable whose
# name ends in _TOKEN, which the generic-api-key rule matches on name+entropy.
# It is not a credential and grants no access.
#
# NOTE: this comment deliberately does NOT reproduce the literal value. The
# previous wording quoted it, which made .gitleaksignore itself a
# generic-api-key hit (fingerprint .gitleaksignore:generic-api-key:2) — an
# ignore file that trips the scanner it configures.
#
# The line-numbered fingerprints below go stale every time the constant moves
# (PR #138 / 6e08cf8 shifted it 44 -> 49 and re-fired the rule on main). The
# durable fix is the path-scoped allowlist in .gitleaks.toml; these entries
# are kept so historical commits stay clean.
58e5f42878564b06c0cfe4eda773a5fbd5485a92:options_researcher/a2_runner.py:generic-api-key:44
6e08cf8061859454c7371560a1fdb92a8ef702d8:options_researcher/a2_runner.py:generic-api-key:49
# 2026-09-02: the PR #146 review receipt quoted the a2_runner.py constant
# verbatim in its first committed form (e584080); the wording was replaced in
# 53b770a but the historical commit remains in the PR range and re-fires.
e5840801ea7d66c2f1fffaa9270dfc4f49bc9fe9:reports/2026-09-02-pr146-ops-fixes-review-receipt.md:generic-api-key:27
# 2026-09-15 (PR #175): the owner fact-append packets and their review
# receipts under reports/2026-09-15-audit/ embed the guarded append commands,
# whose dedupe-prefix variable trips generic-api-key on variable name plus
# entropy. The values are public ledger fact-type labels, not credentials.
# The path-scoped allowlist added to .gitleaks.toml covers this on gitleaks
# 8.30 locally but did NOT on the 8.24.3 that CI pins (run 35034960557
# re-fired all three), so the commit-keyed fingerprints are pinned here too,
# same as the A2 entries above. As above, the literal labels are deliberately
# not reproduced in this comment.
f561db8d4685f4b691e089bc60cc9dc8888cb816:reports/2026-09-15-audit/J-fact-packets-adversarial-review.md:generic-api-key:189
f561db8d4685f4b691e089bc60cc9dc8888cb816:reports/2026-09-15-audit/H-owner-fact-packets.md:generic-api-key:502
f561db8d4685f4b691e089bc60cc9dc8888cb816:reports/2026-09-15-audit/H-owner-fact-packets.md:generic-api-key:739
