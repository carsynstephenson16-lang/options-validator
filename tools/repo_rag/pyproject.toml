[project]
name = "repo-rag"
version = "0.1.0"
description = "Isolated read-only repository RAG application"
requires-python = ">=3.10"
dependencies = []

[tool.uv]
package = false

[tool.ruff]
target-version = "py310"
line-length = 100

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]
