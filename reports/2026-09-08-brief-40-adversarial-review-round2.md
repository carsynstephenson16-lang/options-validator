# Brief 40 — independent adversarial review, round 2 (2026-09-08)

**Reviewer:** Opus subagent dispatched by the orchestrating Claude session (read-only; nothing created, edited, staged or deleted in either checkout; no network).
**Target:** `docs/superpowers/plans/2026-09-08-40-h7-activation-day-receipt-chain-codex-brief.md` rev 2 @ `0cbc777`.
**Base:** `origin/main` @ `863eff2`; `~/options-validator-ops` on `main` @ `863eff2`.
**Method (reviewer's words):** every F1–F15 and every inline WP citation opened at HEAD; `write_artifact`/`write_immutable_receipt`/`validate_durable_receipt`/`_validate_receipt_chain` read end-to-end; `source_hash(FEASIBILITY_SOURCE_PATHS)` and `config_hash()` recomputed against the 09-08 feasibility receipt; `load_registered_cohort()` executed against both stores; `trading_days` executed; import cost of `data.cache_runner` on the capture path measured; all three `EVIDENCE_ALLOW` copies and their pinning tests read; ops working tree and `reports/closes_receipts` mtimes read.
**Orchestrator spot-check (same session, Run-verified):** `tools/ops_alignment_check.sh:62-67` carries the third identical `EVIDENCE_ALLOW`; `tests/test_ops_alignment_check.py:310` and `tests/test_schwab_chain_intraday.py:388` are the equality tests; `reports/pick_tracker` and `reports/closes_receipts` appear in no allow-list; the feasibility receipt's hash fields are `code_sha`, `config_hash`, `receipt_hash`, `source_hash`; `_write_parquet_once` is at `schwab_chain_capture.py:319` (call `:408`); `push_evidence_once` exists in `daily_ritual.sh`.

## Verdict: PASS WITH FIXES

All 15 round-1 findings are genuinely closed in rev 2 — not reinterpreted — and both blockers are correctly fixed. Rev 2 introduced two new blockers of its own (a third allow-list copy with a pinning test; the index committed before it is written), one HIGH that could cost a 15:45 capture, and one HIGH self-contradiction in the exit-code contract. All 11 findings applied in rev 3 (same session).

## Findings (severity · disposition in rev 3)

1. **BLOCKER** · a THIRD `EVIDENCE_ALLOW` copy exists at `tools/ops_alignment_check.sh:62-67` (byte-identical to the other two) and `tests/test_ops_alignment_check.py:310` asserts it equals the pre-close wrapper's; Scope IN listed only two files, so a literal Codex would turn the suite red and leave `ops_alignment_check` publishing `AHEAD_CODE / WILL REFUSE` on activation day. `tests/test_schwab_chain_intraday.py:388` is the intraday equality test and needs no extension. → third file added to Scope IN; both equality tests named; the wrong "find and extend it" sentence removed.
2. **BLOCKER** · WP-B step 5 committed "the step-6 index" but the index was written in step 7, after the commit, into tracked `reports/h7_forward_schwab/` — re-opening R1-2 (`??` file → door refuses). → steps renumbered: index written BEFORE the commit; the print/template step is last.
3. **HIGH** · `--stage-routine-evidence` staged all of `DATA_TIER_PATHS`, which includes `reports/pick_tracker` and `reports/closes_receipts` — in NO `EVIDENCE_ALLOW` list; `reports/closes_receipts` gains a directory every ritual morning. A commit containing them makes the 15:45 capture refuse alignment and lose an irreplaceable capture. → flag restricted to the intersection of `DATA_TIER_PATHS` and `EVIDENCE_ALLOW`; any dirty path outside it is a refusal; intersection property asserted in the shell test.
4. **HIGH** · WP-A step 6's Mode B `exit 1` collided with step 9's exit 1 (included NO_GO) and WP-B step 2's "stop on non-zero" — so a quote-age warning, explicitly "not a registration input", would halt the chain. → Mode B gets exit 3; step 9 table states the receipt is written on 0/1/3 and never on 2; WP-B stops on 1 and 2 only and prints the Mode B warning through.
5. **MEDIUM** · feasibility reuse test named `code_sha` (= git HEAD, never validated by the door) — regenerating after every commit, the ceremony R1-7 removed. The door checks `receipt["source_hash"]` (`h7_schwab_window_registration.py:353`) AND `receipt["config_hash"]` (`:338-340`, `:747`); Run-verified both match the 09-08 receipt today (`d796960a…`). → reuse test = both hashes equal; `code_sha` dropped.
6. **MEDIUM** · the template's `code_commit` is valid only until HEAD next moves (`register_window_real` refuses "evidence code_commit disagrees with HEAD", `:941`); the brief did not say so, and `_assemble_evidence` (`h7_schwab_manual_activate.py:150-162`) overwrites the eight source/data fields, so pre-filling them is redundant. → both stated; runbook prints `git rev-parse HEAD` beside it with a "recompute if you commit anything" line.
7. **MEDIUM** · F15 said the ritual "never pushes"; `daily_ritual.sh:612-623` `push_evidence_once()` does fetch → merge → push with one retry and CRITs on failure. Favourable consequence: an unpushed activation-day evidence commit is pushed by the next morning's Step 8 (fast-forward no-op merge); same-day protection is only `EVIDENCE_ALLOW`, which is why findings 1 and 3 are load-bearing. → F15 corrected; note added to WP-B.
8. **MEDIUM** · `load_registered_cohort()` must be called with its DEFAULT store (`ledger/h7_forward`, `options_researcher/h7_cohort.py:40`); against `ledger/h7_forward_schwab` it raises `CohortUnavailableError … VALID EMPTY`, which is the pre-activation state by construction. F5's "the cohort is read from `load_registered_cohort()`" was inaccurate for the guard, which takes `included` as a parameter (`h7_activation_guard.py:119,154`). → pinned to the no-argument call; identity of that cohort with the Schwab spec's nine labelled Inference; F5 corrected.
9. **MEDIUM** · `data/cache_runner.py:55,67` imports `grpc` and the ThetaData adapter at module level (0.76 s measured; not loaded by the capture today); no circular import. The shell mirror has no python-free calendar; a standalone `uv run python -c` call measured 0.78 s. → WP-C requires a function-local import (precedent `h7_watch.py:241`) and states the measured wrapper cost so the mirror is a decision.
10. **LOW** · citations: `_write_parquet_once` is `:319` (call `:408`), not `:322,340`; fixture `_verified_data_gate_receipt()` begins at `tests/test_h7_schwab_window_registration.py:87` (round 1's `:96` was wrong); no-`--as-of` branch is `h7_source_health.py:233-235`; `FULL_TIER_PATHS` is `daily_ritual.sh:572-574`. → all fixed.
11. **LOW** · D-6 understated the legacy-path hole: `options_researcher/ritual_receipt.py:200` and `options_researcher/h7_entry_preflight.py:26` also hard-code `reports/h7_data_gate`; both are full-authority/post-activation paths. → named in D-6.

## Verified correct by the reviewer (attacked and survived)

The new namespace: `write_artifact(result, reports_dir=X)` yields `X/<scope_id>/<session>.json` (`h7_data_gate.py:761-785`) and `write_immutable_receipt` creates parents (`research/receipts.py:110`); neither `validate_durable_receipt`, `_validate_receipt_chain`, nor the guard reads the receipt's path; the door takes it as an argument; nothing globbing `reports/*` is confused; `restic backup … reports` (`daily_ritual.sh:632`) covers it; `tests/test_h7_backup.py:28,33` use subset assertions so `BACKUP_PATHS` growth breaks nothing; backup receipts land under `reports/h7_receipts/backup{,_restore}/`, already allow-listed. The evidence commit: `DATA_TIER_PATHS` covers today's ops dirt; `alignment_divergence_is_evidence_only` is a tree diff with `--no-renames` and a behind-count refusal, so an allow-listed evidence-only ahead commit passes; `.tmp/` is gitignored (`.gitignore:57`); the one-`git add`-loop-with-stderr-check pattern matches `daily_ritual.sh:594-598`. Feasibility: closure = 50 paths, both hashes match on disk now, the shell comparison is implementable, `--symbols`/`--output` flags exact. F1–F14's substantive claims hold at HEAD, including `_validate_receipt_chain`'s `expected_symbols == scope_identity()["symbols"]` (independently confirms "never pass `scope=`/`symbols=`"), the quote-age signature `evaluate_schwab_quote_age(*, data_gate_receipt, included_symbols)`, F12's `:390/:397/:399` ordering, `shasum -a 256` at `/usr/bin/shasum`; `.cache/schwab_chains` flat; `reports/schwab_chains` holds 09-02, 09-03, 09-07 and no 09-04. Registry row 40 accurate; brief matches the `codex-brief-writing` skill shape.
