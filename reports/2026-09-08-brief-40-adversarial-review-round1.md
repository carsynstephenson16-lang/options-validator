# Brief 40 — independent adversarial review, round 1 (2026-09-08)

**Reviewer:** Opus subagent dispatched by the orchestrating Claude session (read-only; no file created, edited, staged or deleted in either checkout; no network).
**Target:** `docs/superpowers/plans/2026-09-08-40-h7-activation-day-receipt-chain-codex-brief.md` rev 1 @ `df45661`.
**Base:** `origin/main` @ `863eff2` (primary checkout clean; `~/options-validator-ops` on `main` @ `863eff2`).
**Method (reviewer's words):** every F1–F14 and inline citation opened at HEAD; `h7_schwab_data_gate.evaluate()` executed against the real 2026-09-03 package under both chain-dir shapes; `validate_v2_audit_receipt` executed against the Schwab chain path; `feasibility_source_closure()` recomputed; `evaluation_session`/`trading_days` executed; ops capture logs and the installed plist read.
**Orchestrator spot-check (same session, Run-verified):** findings 1 and 2 reproduced independently before adoption — `find .cache/schwab_chains -maxdepth 1 -type d` returns only the root and the 2026-09-03 manifest stores relative paths like `AMD_2026-09-03.parquet`; `tools/h7_schwab_manual_activate.py:66-72` runs `git status --porcelain` without `--untracked-files=no`; `git -C ~/options-validator-ops status --porcelain` already shows `?? reports/intraday_capture/2026-09-08/` and `?? reports/live_probe/2026-09-08.json`. Closure membership recomputed: `options_researcher/h7_watch.py` IS inside `FEASIBILITY_SOURCE_PATHS` (50 entries); `tools/h7_forward_backup.py`, both capture wrappers, `schwab_chain_capture.py`, `h7_source_health.py`, `h7_activation_guard.py` and `h7_schwab_manual_activate.py` are not.

## Verdict: FAIL

The diagnosis (no Schwab-mode receipt writer; the ritual fence; one-day receipt life) is correct and reproduced. The design had two blockers that made the deliverable unsatisfiable as written: WP-A's chain-directory shape does not exist on disk, and every artifact the runbook produces makes the working tree dirty, which is the exact condition the activation door refuses — while the brief forbade the only remedy. All 15 findings applied in rev 2 (same session).

## Findings (severity · disposition in rev 2)

1. **BLOCKER** · `.cache/schwab_chains` is FLAT; the manifest's per-file `path` is relative to it (Run-verified: `chain_dir='.cache/schwab_chains'` → `2026-09-03 GO 15 ['EXACT_SESSION_GO']`; `chain_dir='.cache/schwab_chains/2026-09-03'` → `NO_GO 0 ['SCHWAB_PACKAGE_INVALID']`, `SchwabChainManifestError: missing=[all 15]`). `CACHE_NAMESPACE` (`h7_schwab_window_registration.py:39`) is a namespace string, not a per-session layout; `SESSION_CHAIN_CONVENTION` names the snapshot. WP-A as written exits 2 on 100% of real invocations. → F7 restated; WP-A takes `--chain-dir .cache/schwab_chains` (flat); only `--reports-root` is session-partitioned.
2. **BLOCKER** · the door's `_code_state()` (`tools/h7_schwab_manual_activate.py:66-72`) and the guard's `_working_tree_clean()` (`h7_activation_guard.py:84-90`) count untracked files; every receipt the runbook writes lands in a tracked, non-ignored namespace and appears as `??`; the ops tree is already dirty from routine jobs. WP-B's "empty for tracked files" precondition was strictly weaker than the door's, and the acceptance clause forbade `git add`/`commit`. → F4 restated ("including untracked"); WP-B precondition made byte-identical to `_code_state()`; new WP-B step "evidence commit" stages the exact receipt paths + the ritual's data-tier allow-list and commits once (precedent: `daily_ritual.sh` Step 8; `schwab_chain_capture.sh:84-104` permits HEAD ahead of `origin/main` by evidence-only commits); the "no git add/commit" acceptance clause replaced by "exactly one `git add` loop over an allow-list, one commit, no push"; `reports/h7_forward_schwab` and the new Schwab namespace added to both capture wrappers' `EVIDENCE_ALLOW` so the 15:45 capture does not refuse alignment afterwards.
3. **HIGH** · `evaluate_schwab_quote_age` (`h7_schwab_quote_age_gate.py:183-195`) rejects a raw `evaluate()` result (`receipt_type` is added only by `make_receipt` via `build_receipt`); WP-A steps 4/5 were in the wrong order and would print `QUOTE_AGE_EVIDENCE_INVALID` forever. → build the receipt first, run the quote-age gate on the built receipt, then write; `included_symbols` = the registered nine from `load_registered_cohort()`, not all 15.
4. **HIGH** · `h7_watch.py:111-121` recomputes every GO binding with the ThetaData v2 `validate_v2_audit_receipt` convention (`<cache_dir>/_meta/full_audit.json`); Run-verified against the Schwab path: `REFUSED: v2 full-audit receipt unreadable … FileNotFoundError`. F8's "does not inspect evidence_mode" was true but hid that the watcher cannot consume Schwab evidence at all; and `h7_watch.py` is inside the feasibility closure, so this brief may not edit it. → WP-B step 3 (watcher) DROPPED; F8 restated; recorded as owner item D-6 (post-activation daily procedure has an unrecorded hole: the watcher needs a Schwab-aware binding, a separate brief that will invalidate the feasibility receipt).
5. **HIGH** · writing the Schwab artifact and receipt into the legacy paths collides across evidence modes: `write_artifact` raises `FileExistsError` on differing content (`h7_data_gate.py:768-770`), and `daily_ritual.sh:188-195` reuses whatever receipt sits at `EXPECTED_DG_RECEIPT` when the legacy gate fails — once `h7_active` flips, a same-session ritual replay would adopt the Schwab receipt as the legacy verdict. → own namespace `reports/h7_data_gate_schwab/<scope_id>/{<session>.json, receipts/<session>.json}` via `write_artifact(result, reports_dir=…)` (signature verified `:761`); namespace added to `BACKUP_PATHS` and the restore-check glob in `tools/h7_forward_backup.py` (not in the closure) and to both `EVIDENCE_ALLOW` lists; the door takes the receipt path as an argument (F1) so nothing else moves.
6. **HIGH** · no precondition that a pre-close package exists for `evaluation_session(today)`; on the brief's own date it does not (`evaluation_session(2026-09-08) = 2026-09-04`; 09-04 was refused; 09-07 is not a session). WP-B would have written an immutable source-health receipt and dirtied the tree before discovering `SCHWAB_PACKAGE_INVALID`. → WP-B step 0 precondition: refuse with `NO_PACKAGE_FOR_SESSION` if `reports/schwab_chains/<session>/{manifest,preclose}.json` are absent, before anything is written.
7. **MEDIUM** · F14 over-broad: `feasibility_source_closure() == FEASIBILITY_SOURCE_PATHS` (50 files); no module WP-A/B/C edits is inside it (`h7_schwab_data_gate.py`, `h7_data_gate.py`, `research/receipts.py` are inside but are only imported); the measurement reads the frozen `.cache/chains` and is time-invariant. WP-B step 0's unconditional regeneration was ceremony that manufactured an untracked file. → F14 restated ("edits inside the 50-path list invalidate it; a new `tools/` module does not"); regeneration conditional on `source_hash(FEASIBILITY_SOURCE_PATHS)` differing from the newest receipt's; included in the evidence commit.
8. **MEDIUM** · `h7_source_health` exits 1 whenever any of the 15 is unhealthy (`:290-295`); six excluded names are permanently `EARNINGS-UNKNOWN`, so exit 1 is the steady state (the ritual treats it as non-fatal, `daily_ritual.sh:164-167`). Also `--as-of` is required or the receipt's `evaluation_session` becomes today and the gate rejects it (`h7_data_gate.py:631`). → stop condition = per-name `symbols[<sym>].healthy is False` for any of the nine (guard rule `h7_activation_guard.py:225-236`), exit code ignored; `--as-of <today>` mandatory.
9. **MEDIUM** · `SCHWAB CHAIN REFUSED` is composed by the wrapper (`tools/schwab_chain_capture.sh:150`) from a grep of `^schwab_chain_capture refused:`; the python has no reason-code taxonomy (`:390`, `:453`). → WP-C contract: print `schwab_chain_capture refused: NOT_A_TRADING_SESSION <session>` at line start, return 1; no new top-level string.
10. **MEDIUM** · session is derived at `schwab_chain_capture.py:399` AFTER `_default_client()` at `:397`; no `--session` arg; precedent test `tests/test_schwab_chain_capture.py:216`; the first-write-wins slot is `_write_parquet_once` (`:322,340`). → refusal placed before client construction; test file named (single file, not a family); slot named precisely.
11. **MEDIUM** · `--evidence` is required (`h7_schwab_manual_activate.py:343`) and `EVIDENCE_FIELDS` (`h7_schwab_window_registration.py:227-245`) demands fields no tool assembles (`review_evidence`, `code_commit`, `last_historical_manifest_receipt_hash`, `feasibility_receipt`, `feasibility_receipt_hash`, `darwin_durability_verified`, `pre_append_state`); "every path filled in" was false. → WP-B emits an evidence TEMPLATE under `.tmp/` with the machine-derivable fields filled and `<OWNER TYPES>` for the rest; the printed command carries the template path with an explicit "complete before use" line.
12. **MEDIUM** · plist-parsing cwd precondition has no precedent and fails for no benefit; strict `HEAD == origin/main` is stricter than the cited wrapper (`schwab_chain_capture.sh:36-52`, `:84-104` permits evidence-only ahead commits) and incompatible with finding 2's commit. → repo root resolved from the script path, `--allow-checkout <path>` otherwise; alignment evaluated once before any artifact, evidence-only divergence permitted with the wrapper's own rule; the script prints the realign push command and never pushes.
13. **MEDIUM** · no explicit IN/OUT Scope section (skill body item 2). → `## Scope` added; the dispatch prompt now restates rather than originates the OUT list.
14. **MEDIUM** · WP-A's exit-0-on-included-nine policy diverges from the legacy CLI's exit-0-on-whole-universe (`h7_data_gate.py:893`); defensible under trim mode but an owner-visible contract. → lifted to owner decision D-5 with a recommended default; cohort read from `load_registered_cohort()`, never hard-coded.
15. **LOW** · `tests/test_daily_ritual_staging.py` does not exist (Brief 38's shell tests are in `tests/test_daily_ritual_provenance.py`); sentinel is `daily_ritual.sh:186`; included-subset computation is `h7_activation_guard.py:176-180`; fixture `_verified_data_gate_receipt()` starts at `tests/test_h7_schwab_window_registration.py:96`; constants at `h7_schwab_window_registration.py:38-39`; WP-A.1's trading-day refusal is dead code (`evaluation_session()` returns a trading day by construction, `h7_watch.py:241-246`); 2026-09-08 is a trading day, the 09-04 shift is the "strictly before" rule; the real reason an all-NO_GO package receipt is never written is that `build_receipt` cannot build it (`validate_receipt_scope_closure` raises `data-gate result lacks a verified Schwab package`, `h7_schwab_data_gate.py:44-51,104-108`). → all fixed.

## Verified correct by the reviewer (attacked and survived)

The core diagnosis: `h7_schwab_data_gate.evaluate()` has no production caller (non-test references are the registration module's constant/closure list, the quote-age gate's helper import, `h7_data_gate.py:522,527,601,604`, and a docstring); the legacy CLI evaluates `.cache/chains` in legacy mode. F1 flags verbatim at `:343-351`; `_validate_receipt_chain` `:120-147` and `_validate_backup` `:108-117` enforce exactly what F2/F3 claim; trim mode as F5 states (`h7_activation_guard.py:106-…`, door mirror `:662-677`); `build_receipt` (`:610-667`) works with the Schwab result because `_validate_result_scope_closure` branches on `EVIDENCE_MODE` at `:600-606`; `EVIDENCE_MODE` set once at `h7_schwab_data_gate.py:15`, enforced at `:640` and `:699`; `evaluate()` signature exact, `scope=None, symbols=None` yields the 15-name universe (`:151-153`); F9, F10, F11, F12, F13 exact; the nine-name cohort Run-verified from `load_registered_cohort().included`; `data.cache_runner.trading_days(start, end)` at `:121-124`; `BACKUP_PATHS` covers `reports/h7_data_gate` + `reports/h7_receipts` and the restore-check scans `*/receipts/*.json`, so backup-after-receipts is load-bearing and correct; Brief 36's ordering rule at `:74-83`; `ritual_authority.py:39,82-83`; the 2026-09-07 holiday package is tracked in both checkouts and `evaluation_session()` cannot select it; registry row 40 present; header order correct; and a whole-universe GO is achievable today against the 2026-09-03 package (`GO 15/15 EXACT_SESSION_GO`) — the 2026-09-08 blocker is the earnings store (D-1) and the missing 09-04 package, not the data gate.
