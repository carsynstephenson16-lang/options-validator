[project]
name = "options-validator"
version = "0.1.0"
description = "Options strategy validation harness for testing expectancy after realistic costs."
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
    "lumibot>=4.5.63",
    "numpy>=1.26,<3",
    "pandas>=2.2,<3",
    "pyarrow>=14,<22",
]

[tool.uv]
package = false
